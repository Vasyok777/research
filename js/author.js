// document.addEventListener('DOMContentLoaded', function () {
// 	const buttons = document.querySelectorAll(
// 		'.author-info__bottom-buttons button'
// 	)
// 	const icons = document.querySelectorAll('.icons__item')
// 	const iconsText = document.querySelector('.icons__text')
// 	const hiddenButtons = document.querySelector('.hidden-buttons')

// 	if (window.innerWidth <= 900) {
// 		buttons.forEach((button, index) => {
// 			if (index >= 3) {
// 				button.style.display = 'none'
// 			}
// 		})
// 		if (window.innerWidth <= 900) {
// 			icons.forEach((icon, index) => {
// 				if (index >= 6) {
// 					icon.style.display = 'none'
// 				}
// 			})
// 		}

// 		hiddenButtons.textContent = `+${buttons.length + 2} more`
// 		iconsText.textContent = `+${icons.length + 7} more`
// 	}
})
